// ============================================================
// Session Comparison Rule -- Unit Tests
// ============================================================

import { describe, it, expect, vi } from 'vitest';
// import { SessionComparisonRule } from '../../src/main/coaching/rules/session-comparison';
// import type { RuleContext } from '../../src/main/coaching/types';

describe('SessionComparisonRule', () => {
  it.todo('should emit achievement when kills are 15%+ above weekly average');
  it.todo('should emit warning when kills are 15%+ below weekly average');
  it.todo('should emit nothing when session has fewer than 2 matches');
  it.todo('should emit nothing when no daily aggregates exist');
  it.todo('should emit info for neutral performance (-5% to +5%)');
  it.todo('should compare damage independently from kills');
});
